<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <androidx.appcompat.widget.LinearLayoutCompat
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            tools:context=".report.controller.ReportWebActivity">

        <WebView
                android:id="@+id/report_web_view"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="0.875"/>

        <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/web_view_submit_button"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="0.125"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintRight_toRightOf="parent"
                android:text="@string/click_to_submit_on_web_view_page"
                android:textSize="22sp"
                android:onClick="onWebViewSubmitButtonClicked"
                android:enabled="false"
                android:visibility="gone"/>

    </androidx.appcompat.widget.LinearLayoutCompat>
</layout>
